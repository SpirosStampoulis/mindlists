# Dependencies
node_modules/

# Build & Vite preview
dist/
dist-ssr/
build/
dev-dist/
.vite/
*.local

# TypeScript
*.tsbuildinfo

# Environment & secrets (never commit real keys)
.env
.env.local
.env.*.local

# Logs & debug
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Firebase (CLI cache, emulator logs)
.firebase/
*-debug.log
firestore-debug.log

# Tests & coverage
coverage/
.nyc_output/

# Editors & IDEs
.idea/
*.swp
*.swo
*~

# VS Code: ignore workspace noise, allow shared settings if you add them
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# OS
.DS_Store
Thumbs.db

# Optional: keep personal receipt JSON out of git (remove # to enable)
# receipts/*.json
# !receipts/.gitkeep
